export class Ship {
    type;
    shape;
    position;
    orientation;
    constructor(type, shape, position, orientation) {
        this.type = type;
        this.shape = shape;
        this.position = position;
        this.orientation = orientation;
    }
}
